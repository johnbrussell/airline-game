<h1>Airline Game</h1>
<p><a href=<%=game_path(@game)%>>Return to game overview</a></p>
<h2>There <%="is".pluralize(@airlines.count)%> <%= @airlines.count %> <%="airline".pluralize(@airlines.count)%> in the game.</h2>

<ul>
  <% @airlines.each do |airline| %>
    <li>
      <a href=<%=game_airline_path(@game.id, airline.id) %>><%= airline.name %></a>
    </li>
  <% end %>
</ul>
